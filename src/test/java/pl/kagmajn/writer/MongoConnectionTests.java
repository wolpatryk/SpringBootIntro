package pl.kagmajn.writer;

import org.junit.Test;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public class MongoConnectionTests {
    public static final Logger log = LoggerFactory.getLogger(MongoConnectionTests.class);
    @Test
    //TODO make tests in the future
    // ./gradlew clean build
    // ./gradlew test --tests --info pl.kagmajn.writer.MongoConnectionTests
    public void shouldWorkTest(){
        log.info("test");
    }
}
